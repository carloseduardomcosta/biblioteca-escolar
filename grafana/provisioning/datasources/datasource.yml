apiVersion: 1
datasources:
  - name: Biblioteca MySQL
    type: mysql
    access: proxy
    url: db:3306
    database: biblioteca
    user: ${MYSQL_USER}
    secureJsonData:
      password: ${MYSQL_PASSWORD}
    isDefault: true
